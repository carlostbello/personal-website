# Environment Variables Example
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to git!

# ================================================================================
# PUBLIC ENVIRONMENT VARIABLES (exposed to browser)
# Use NEXT_PUBLIC_ prefix only for values that are safe to expose to clients
# ================================================================================

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
NEXT_PUBLIC_SITE_NAME="Your Name"

# Analytics (Optional)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ================================================================================
# PRIVATE ENVIRONMENT VARIABLES (server-side only)
# These are never exposed to the browser
# ================================================================================

# Email Service (for contact form)
# Example: SendGrid, Resend, or other email service
# EMAIL_API_KEY=your_email_api_key
# EMAIL_FROM=noreply@yourdomain.com
# EMAIL_TO=your@email.com

# Database (if needed)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Authentication (if implemented)
# NEXTAUTH_SECRET=your_nextauth_secret
# NEXTAUTH_URL=http://localhost:3000

# ================================================================================
# DEVELOPMENT ENVIRONMENT VARIABLES
# Only needed for local development
# ================================================================================

# Next.js MCP Server (Next.js 16+: enabled by default, < 16: set this)
# __NEXT_EXPERIMENTAL_MCP_SERVER=true

# ================================================================================
# NOTES
# ================================================================================
#
# 1. Copy this file to .env.local for local development
# 2. Add production values to Vercel dashboard for production/preview deployments
# 3. Never commit .env.local or any file containing actual secrets
# 4. Update this file when adding new environment variables
# 5. Document what each variable is used for
#
# ================================================================================
# VERCEL DEPLOYMENT
# ================================================================================
#
# For Vercel deployment, add environment variables in the Vercel dashboard:
# 1. Go to Project Settings → Environment Variables
# 2. Add each variable for Production, Preview, and Development as needed
# 3. Variables starting with NEXT_PUBLIC_ are exposed to the browser
# 4. Other variables are only available server-side
#
# ================================================================================
