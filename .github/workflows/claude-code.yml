name: Claude Code CI/CD

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  # Claude Code Review - OPTIONAL: Requires ANTHROPIC_API_KEY secret
  # Uncomment and add ANTHROPIC_API_KEY secret to enable
  # claude-review:
  #   runs-on: ubuntu-latest
  #   if: |
  #     (github.event_name == 'pull_request') ||
  #     (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude'))
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Claude Code Review
  #       uses: anthropics/claude-code-action@v1
  #       with:
  #         anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
  #         github_token: ${{ secrets.GITHUB_TOKEN }}

  # Security Scan - OPTIONAL: Requires ANTHROPIC_API_KEY secret
  # Uncomment and add ANTHROPIC_API_KEY secret to enable
  # security-scan:
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'pull_request'
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Security Review
  #       uses: anthropics/claude-code-action@v1
  #       with:
  #         anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
  #         github_token: ${{ secrets.GITHUB_TOKEN }}
  #         mode: prompt
  #         prompt: |
  #           Run a comprehensive security review following OWASP guidelines.
  #           Check for:
  #           - Exposed secrets or API keys
  #           - Authentication/authorization issues
  #           - Input validation problems
  #           - XSS/CSRF vulnerabilities
  #           - Insecure dependencies
  #           Provide specific line numbers and remediation steps.

  lint-and-test:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Type check
        run: npm run type-check

      - name: Test
        run: npm test
        continue-on-error: true  # Allow to pass if no tests yet

      - name: Build
        run: npm run build

      - name: Check for dependency vulnerabilities
        run: npm audit --audit-level=high
        continue-on-error: true  # Don't fail build, but show warnings

  # Vercel Deployment - OPTIONAL: Use Vercel's GitHub integration instead
  # This is more efficient and doesn't use GitHub Actions minutes
  # Install Vercel GitHub App: https://vercel.com/integrations/github
  #
  # Uncomment these jobs only if you want to use GitHub Actions for deployment
  # deploy-preview:
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'pull_request'
  #   needs: [lint-and-test]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Deploy to Vercel Preview
  #       uses: amondnet/vercel-action@v25
  #       with:
  #         vercel-token: ${{ secrets.VERCEL_TOKEN }}
  #         vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
  #         vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
  #         scope: ${{ secrets.VERCEL_ORG_ID }}

  # deploy-production:
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Deploy to Vercel Production
  #       uses: amondnet/vercel-action@v25
  #       with:
  #         vercel-token: ${{ secrets.VERCEL_TOKEN }}
  #         vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
  #         vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
  #         vercel-args: '--prod'
  #         scope: ${{ secrets.VERCEL_ORG_ID }}
